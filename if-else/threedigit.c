#include <stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    if(n<=999){
        printf("three digit number");
    }
    else{
        printf("not three digit number");
    }
    return 0;
}